<% events.each do |event| %>
  <!-- <%= events.size %> -->
  <!-- <%= events.first.convert? %> -->
  <!-- <%= events.first.cookies.inspect %> -->
  <% if event.convert? %>
    <img src="https://click.exacttarget.com/conversion.aspx?xml=#{event.tags}" style="display: none;" />
    <%= event.clear %>
  <% end %>
<% end %>

